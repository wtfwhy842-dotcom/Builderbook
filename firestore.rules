rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write for the prototype.
    // In production, we'd add auth checks.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
